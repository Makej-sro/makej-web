# STAV — aktuální kontext projektu

> Tenhle soubor je „kde právě jsme". `CLAUDE.md` = neměnný popis projektu (architektura, DB, konvence).
> `STAV.md` = co se řešilo naposledy, na co si dát pozor, co je rozdělané.
> **Po dokončení každé větší práce sem AI zapíše 2–3 řádky.** Drž to stručné.

---

## Jak pracovat s tímhle projektem (důležité pro úsporu kontextu)

- **Zakládej nové chaty místo jednoho dlouhého.** `/compact` konverzaci nemaže, jen ji zabalí do shrnutí, které se táhne dál — čím déle chat běží, tím víc tokenů se přenáší. Nový chat = jen `CLAUDE.md` + `STAV.md`.
- Nový chat automaticky přečte `CLAUDE.md` (a přes odkaz i tenhle soubor). Nemusíš nic vysvětlovat od nuly.
- Lokální servery: web `python3 -m http.server 3333` ve složce `makej-web`, mobilní app `npm run dev` ve složce `makej`.

## Kontrola JSX v dashboardu (Babel Standalone běží v prohlížeči)

JSX soubory ve `employer/` nemají build — validují se ručně parserem:
```bash
node -e 'const p=require("/Users/samuelpseja/cursor/makej/node_modules/@babel/parser");
const fs=require("fs"); p.parse(fs.readFileSync("employer/employer-main.jsx","utf8"),
{sourceType:"script",plugins:["jsx"]}); console.log("OK")'
```
Po změně JSX **vždy bumpni `?v=N`** u daného souboru v `employer/index.html` (cache-busting), jinak prohlížeč načte starou verzi.

## Dashboard je inline-styled React (žádné CSS classy)

- Komponenty používají výhradně `style={{}}` — media query je nepřebijí přes classy.
- Responzivita řešena dvěma způsoby:
  1. **JS hook `useIsMobile()`** (definován v `employer/employer-theme.jsx`, breakpoint `MOBILE_BP = 820`) — pro logiku (drawer sidebar, přepínání panelů).
  2. **Globální CSS v `employer/index.html`** s `!important` atributovým selektorem (`[style*="repeat(4, 1fr)"]` apod.) — přebíjí inline gridy a stackuje je na mobilu. Když přidáš nový vícesloupcový grid, buď použij existující `repeat(N, 1fr)` string (chytne ho selektor), nebo přidej pravidlo.

---

## Hotovo naposledy

- **Chat: přílohy + hlasovky (makej-aplikace)** (2026-07-22): DB/storage (viz `migration_chat_attachments.sql`): sloupce v `messages` (file_url/file_type/file_name/file_size/duration), **private** bucket `chat-prilohy` (25 MB), RLS storage.objects — přístup jen účastníci matche (cesta `{match_id}/...`, přes signed URL), ověřeno. **Appka** (`makej-aplikace`): `worker-supabase.jsx` → `uploadChatFileW` (upload do `{matchId}/...`, fotky se zmenší) + `chatSignedUrlW` (createSignedUrl 1 h); zprávy nesou file pole (fetchWorkerData i realtime mapování). `worker-messages.jsx`: input má tlačítko **přílohy** (📎, auto-detekce fotka vs soubor dle mime) a **mikrofon** (🎤, přepne se na odeslání při psaní); hlasovky přes **MediaRecorder** (timer, zrušit/odeslat). Bubliny: fotka inline, hlasovka = audio přehrávač + délka, soubor = karta ke stažení. Signed URL se předgenerují pro otevřenou konverzaci (private bucket). Ikony 📎/🎤 emoji (worker Solar sada je nemá). Keyframe `wRecPulse`. Ověřeno v prohlížeči (bubliny + input renderují, 0 chyb). Bump `worker-supabase?v=7`, `worker-messages?v=2`. **Employer strana hotová** (obousměrné): `employer-supabase` (uploadChatFileE/chatSignedUrlE + file pole do E_THREADS) + `employer-pages3` EMessages (📎 příloha + 🎤 hlasovka, bubliny přes signed URL); bump `employer-supabase?v=28`, `pages3?v=28`, keyframe `empRecPulse`. **Bod 1 (notify trigger) byl už hotový.**

- **Nahrávání obrázků: profilovky, fotky inzerátů, logo/fotky firmy (Supabase Storage)** (2026-07-19): **Migrace `migration_storage_uploads.sql`** (aplikováno): public bucket `uploads` (5 MB, jen image typy), RLS na `storage.objects` — veřejné čtení, zápis/mazání jen do vlastní složky (`{auth.uid}/…`, ověřeno `storage.foldername`), + `jobs.photos` jsonb. Do DB se ukládá jen veřejná URL. **Upload helpery** (canvas resize → JPEG → `sb.storage.upload` → `getPublicUrl`): `uploadImageW` (worker-supabase.jsx), `uploadImageE` (employer-supabase.jsx). Cesta `{uid}/{prefix}-{ts}-{rand}.jpg`. **Brigádník — profilovka** (`avatar_url`): upload v onboardingu (intro avatar) i v profilu (edit mód, tužka overlay), zobrazí se místo iniciál; profil firmy o kandidátovi (`EWorkerProfileModal`) už `avatar_url` zobrazoval. **Firma — fotky inzerátu** (galerie do 8, `jobs.photos`): upload v job formu (thumbnaily + ✕), první fotka = pozadí swipe karty (`heroImg` v worker-swipe.jsx, `jobToCard.photos`). **Firma — logo + profilovka + fotky firmy**: `ImageField` v `ESettings` (employer-pages3.jsx) předělán z URL inputu na **upload** (`onUpload`), „Bonusové fotky" = upload galerie; zobrazí se v `WEmployerModal` (profil firmy u brigádníka). Storage plumbing ověřeno REST (upload/read/delete 200), UI renderuje bez chyb, RLS cesta sedí. Bump: worker `supabase?v=6/profile?v=4/swipe?v=6/onboarding?v=4`, employer `supabase?v=26/main?v=29/pages3?v=26`. **Pozn.:** e2e upload s reálným loginem ověří user (storage RLS chce přihlášeného).


- **Toast notifikace v employer dashboardu — náš styl** (2026-07-19): `EToast` (`employer-main.jsx`) měl **tmavé pozadí `rgba(16,16,42,0.97)` + tmavý `T.ink` text** (dashboard je teď světlý) → titulek skoro nečitelný. Předěláno do stylu jako v brigádnické appce: světlá karta (jemný barevný gradient → bílá), barevný box s ikonou (accent tint dle typu), tučný titulek (`T.fontHead`/Inter) + muted text, ✕ s hoverem, **odpočtový proužek** dole v barvě typu (`@keyframes empToastBar` v employer/index.html). `E_TOAST_STYLE` mapa typů: `info` modrá `#0020F6`, `success` zelená `#16a34a`, `warn` oranžová `#F5A623`. Notif handler: potvrzení směny → `success`, zrušení → `warn`. Ověřeno v prohlížeči (3 vzorky renderují čistě). Bump `employer-main?v=27`.
- **Kalendář místo tabu „Brigády" (z Yasina)** (2026-07-19): druhý tab v appce je teď **„Kalendář"** místo „Brigády". Zkopírován `worker-calendar.jsx` z Yasinova repa (`WCalendar` — měsíční přehled brigád z `W_HISTORY`: tečky u dnů dle fáze, dnešek zvýrazněný, výběr dne filtruje seznam, měsíc/rok přes „revolver" `WWheel`, přepínání měsíce potažením). Uvnitř renderuje `<WHistory embedded />` (plný seznam brigád pod kalendářem, když není vybrán den) — do našeho `worker-history.jsx` přidán prop **`embedded`** (skryje vlastní nadpis/scroll/odsazení; jen strukturální, naše barvy zachovány). `worker-main.jsx`: NAV tab `history` → label „Kalendář" + ikona `calendar-minimalistic-bold`, render `<WCalendar>`. `index.html`: přidán skript `worker-calendar.jsx`, CSS `.w-wheel` (skrytí scrollbaru). Yasinovy barvy `#0020F6`/`rgba(0,32,246)` sedí s tím, co appka už používá. Ověřeno v prohlížeči: měsíční mřížka, tečky, výběr dne + „Zpět na vše", vnořený seznam. Bump `worker-main?v=7`, `worker-history?v=2`, `worker-calendar?v=1`.
- **Radius místo filtru krajů (poloha + km)** (2026-07-19): brigádník si v onboardingu zvolí kraj → **město** (z tabulky `cities`) → **radius** (10/25/50/100 km nebo „celá ČR"); firma u inzerátu vybere **město ze seznamu** → job dostane `lat/lng`. Feed pak ukazuje jen brigády do zvoleného radiusu (haversine z uložených souřadnic), na kartě vzdálenost. **Migrace `migration_cities_radius.sql`** (aplikováno): tabulka `cities` (id/name/kraj/lat/lng, RLS select pro všechny) se **87 městy** — Praha rozdělena na Praha 1–10, Brno jen `Brno-město`/`Brno-venkov`, ostatní města jednou položkou; sloupce `profiles`: `city/lat/lng/work_radius_km`. **Onboarding** (`worker-onboarding.jsx`): krok „kde jsi" = kraj select → město select (z DB, filtr dle kraje) → radius chipy; ukládá city/lat/lng/work_radius_km; `wOnboardingNeeded` nově vyžaduje i `city`. **Feed** (`worker-swipe.jsx` + `_wDistanceKm` haversine v `worker-supabase.jsx`): filtr krajů nahrazen radiusem (bez polohy / „celá ČR" = vše; joby bez souřadnic vždy vidět), radius chip nahoře ukládá `work_radius_km` do profilu, `WDeckEnd` „rozšířit → celá ČR". **Firma** (`employer-main.jsx` job form): „Kraj" select nahrazen „Město / část" (optgroup dle kraje, z `cities`), odvozuje kraj + lat/lng; „Místo" volný text nepovinný (fallback = název města); `createJobE/updateJobE` ukládají `lat/lng`. Ověřeno v prohlížeči: onboarding kraj→město kaskáda, radius filtr (Praha 4 r25 → Brno 186 km skryto, Praha vidět), `_wDistanceKm` (Praha↔Brno=186 km), anon čte `cities`, parser OK. Bump `worker-supabase?v=5`, `worker-swipe?v=3`, `worker-onboarding?v=3`, `employer-main?v=26`, `employer-supabase?v=25`. **Pozn.:** město-level (bez geokódování adresy) — vzdálenost město↔město; malé obce → nejbližší uvedené město.
- **Automatické stažení brigád po datu (neaktivní pro obě strany)** (2026-07-19): 1) **Klient** (`worker-supabase.jsx` fetchWorkerData) — do feedu se vůbec nenačtou brigády po datu konání (`_wJobPassed(_wResolveEventDate(j, null))`), takže je brigádník nevidí ani bez swajpnutí. Bump `worker-supabase.jsx?v=4`. 2) **Server** (`supabase/migration_expire_past_jobs.sql`, aplikováno přes Management API) — zapnut **pg_cron**, funkce `expire_past_jobs()` přepne aktivní joby po datu na status `'expired'` (už existoval v constraintu), naplánováno **denně 00:10 UTC** (`cron.schedule 'expire-past-jobs'`). `'expired'` je neaktivní pro obě strany: worker feed bere jen `status='active'`, employer dashboard mapuje `'expired'→'paused'` (`employer-supabase.jsx:239`). Pomocná fce `_job_event_date` bezpečně parsuje event_date / ISO date / legacy 'D.M' (s ošetřením chyb). Spuštěno jednorázově teď → **7 prošlých jobů přepnuto na expired** (0 zbylo active — všechny testovací byly po datu; pro test swipu je potřeba vytvořit job s budoucím datem).
- **Zvuk upozornění + „Prohlédnout odmítnuté" (z Yasinova repa)** (2026-07-19): přeneseno z `github.com/Makej-sro/makej-aplikace-yasin` do `makej-aplikace`. 1) **Zvuk upozornění** — syntetizovaný přes Web Audio (žádný MP3, offline): `_wAudio/_wStrike/wPlayBell/wSoundEnabled` + odemykací listenery (touch/click/visibilitychange kvůli iOS) v `worker-main.jsx`; přehrává se v `addToast` (dvojité rAF) + rozkývání zvonečku (`bellRing` stav + `@keyframes wBellRing` v index.html). Přepínač **Zvuk** v profilu (`worker-profile.jsx`, localStorage `makej-notif-sound`, cinkne při zapnutí), zobrazený jen když jsou upozornění zapnutá. **Nebrán Yasinův `insertNotifW`** (klientský insert) — my máme triggery + klientský insert je v DB zakázaný. 2) **Konec zásobníku `WDeckEnd`** (`worker-swipe.jsx`) — když projdeš všechny brigády: pokud máš filtr krajů → „Zobrazit N brigád odjinud" (zruší filtr), jinak když máš odmítnuté → **„Prohlédnout již odmítnuté"** (`fetchRejectedJobsW` v `worker-supabase.jsx` načte aktivní odmítnuté a nahraje je zpět do stacku), fallback „Zkusit znovu". Ikony `volume-loud-bold`/`refresh-bold` (Yasin má vlastní sadu) nahrazeny dostupnými (`bell-bold`, bez ikony). Ověřeno v prohlížeči: parser OK, 0 chyb, obě varianty WDeckEnd + přepínač zvuku renderují, `wPlayBell()` běží. Bump `worker-main?v=4`, `worker-supabase?v=3`, `worker-profile?v=3`, `worker-swipe?v=2`.
- **Duplicitní notifikace u brigádníka + activity feed u firmy** (2026-07-19): 1) Firma viděla v „Aktivitě" 4× „X poslal/a zprávu" — feed bral posledních N zpráv a všechny označil jménem brigádníka, i firmou odeslané. Fix `employer-supabase.jsx` (E_ACTIVITY): jen PŘÍCHOZÍ zprávy (`sender_id !== employerId`), max 1 na konverzaci. Bump `employer-supabase.jsx?v=24`. 2) Brigádník dostával každou zprávu 2× (+ vracelo se „Testovací upozornění"). Příčina: vedle serverového triggeru vkládala notifikace i **stará nacachovaná verze appky** klientsky (moje 1. implementace + test funkce). **Neprůstřelný fix na úrovni DB:** zrušeny všechny INSERT policy na `notifications` → **klient vložit nemůže, smí jen SECURITY DEFINER triggery**. Ověřeno: trigger dál vkládá (1 řádek), duplikáty i test rows smazané (0 dup skupin). Migrace `migration_notifications_persist.sql` upravena (bez insert policy). Tím je jedno, jestli běží stará appka — duplikát nevznikne.


- **Perzistentní upozornění brigádníka — serverové triggery (makej-aplikace)** (2026-07): upozornění (zvoneček) teď **přežijí zavření/refresh appky** a chodí **i když je appka zavřená** (server je vytvoří, appka je při dalším otevření načte). Tabulka `notifications` (`id, user_id→profiles, type, match_id→matches, title, body, read, created_at`; zjištěno přes PostgREST OpenAPI service_role klíčem). **Migrace `supabase/migration_notifications_persist.sql`** (spustit v SQL Editoru — MCP/SQL exec tu není k dispozici): 1) rozšíření CHECK na `type` na 6 hodnot `message/match/shift/review/success/info` (původně jen `message`), 2) RLS (uživatel čte/upravuje/maže jen svá, `auth.uid()=user_id`), 3) index `(user_id, created_at desc)`, 4) **triggery** (SECURITY DEFINER) — `notify_on_message` (zpráva/`shift_offer`/`interview_offer` → příjemci matche), `notify_on_match_accepted` (match→accepted → 'match' brigádníkovi), `notify_on_review` (recenze → 'review' hodnocenému), 5) `notifications` přidána do `supabase_realtime` publikace. **Architektura: triggery = zdroj pravdy, klient jen čte + poslouchá realtime** (žádné client-side inserty → žádné duplicity). Kód: `worker-supabase.jsx` → `fetchNotificationsW/markNotificationsReadW/clearNotificationsW` + `_wNotifKind` (match/message/shift→chat, review→history); `createNotificationW` ponechán (nevyužitý, pro budoucnost). `worker-main.jsx`: login načte historii (avatar dopočítá z `W_THREADS` dle `match_id`); realtime handlery `matches`/`messages` už jen **refreshují data** (notif/toast z nich odebrán); **nový odběr `notifications`** (filter `user_id=eq.<id>`) → zvoneček (dedupe dle `dbId`) + toast (potlačen na tabu Zprávy u chat notifikací); zvoneček open → `markNotificationsReadW`, „Vymazat" → `clearNotificationsW`. **Migrace APLIKOVÁNA** (2026-07, přes Supabase Management API + PAT — `POST /v1/projects/{ref}/database/query`, běží jako `postgres`). Ověřeno naživo: constraint = 6 typů, 3 triggery na messages/matches/reviews, RLS policy, realtime publikace. **Triggery otestovány** bezpečně v transakcích s `rollback` (reálná data nedotčena — match zůstal pending, žádné test zprávy/recenze): zpráva→`message`, shift_offer→`shift`, match accepted→`match`, review→`review`, vždy správnému příjemci s názvem firmy v titulku. Tabulka `notifications` vyčištěna (0 řádků). Klient: parser OK, appka běží bez chyb. Přidán 4. trigger `notify_on_shift_confirmed` (match→confirmed → **obě strany**: brigádník „Brigáda potvrzená ✅", firma „Směna potvrzená ✅"), otestován rollbackem. **DUPLIKÁTY zjištěny při testu v appce:** vedle mých triggerů tvoří notifikace i **Next.js appka `cursor/makej`** (klientsky) — texty „Testovací upozornění" (ještě 2× po sobě, patrně StrictMode) a „Samuel ti **nabízí** směnu" (moje trigger varianta je „nabídl/a"). Tyhle texty nejsou nikde v `makej-aplikace`/`makej-web` → sedí jen v `cursor/makej`, který **není v tomto workspace** (working dirs jen Makej-projekt + /tmp). **Rozhodnuto (user): zdroj = serverové triggery.** Ukázalo se, že `cursor/makej` (Next.js) už neexistuje (ani na GitHubu) a **jediná appka je `makej-aplikace`**, běží na localhostu. Ověřeno: **aktuální kód `makej-aplikace` NEVYTVÁŘÍ notifikace na klientu** — `createNotificationW` mělo 0 volání → **odstraněno** (jediné zápisy do `notifications` jsou teď fetch/mark-read/clear). Texty „Testovací upozornění"/„nabízí" nejsou v žádném přístupném kódu → pocházely ze **staré/nacachované verze v prohlížeči nebo ručních test insertů**; s aktuálním kódem se dít nebudou. **Cache-busting:** do `index.html` přidáno `?v=2` u worker-supabase/profile/onboarding/main (index.html načítal .jsx bez verze → prohlížeč držel starý kód, což byl nejspíš zdroj duplicit). Přidán 4. trigger `notify_on_shift_confirmed` (obě strany), otestován. **Stav: server hotový, klient jen čte, ověřeno reálným testem (single, správné notifikace, 0 duplicit/test spamu).**

- **Chat z notifikace se neotevřel bez restartu** (2026-07): `openChat` v `worker-main.jsx` teď při chybějícím vláknu (nová shoda/potvrzení přišlo realtime dřív než refresh) nejdřív přepne na Zprávy, pak dotáhne data (`refreshWorker`) a otevře chat → bez restartu. `index.html` cache-bust `worker-main.jsx?v=3`.
- **Employer dashboard realtime** (2026-07): firma neviděla nové kandidáty bez refreshe — příčina: matches realtime se k firmě nedoručí (RLS přes `jobs`+`can_act_as` realtime nevyhodnotí; worker má triviální `worker_id=auth.uid()`, proto jemu chodí). Řešení: **2 nové triggery** `notify_on_match_created` (match INSERT → firmě 'match' „Nový/Superzájem o brigádu") a `notify_on_shift_cancelled` (match→cancelled → firmě 'info' „Zrušená směna"), a **employer-main.jsx** se napojil na **kanál `notifications`** (filter `user_id=eq.<empId>`, triviální RLS → spolehlivé doručení) → `fetchEmployerData`+`setTick`+toast; z původních matches handlerů odebrán toast (nechán jen záložní refresh); u zrušení otevře cancel modal. Celkem **6 notify triggerů** (message, match_accepted, match_created, shift_confirmed, shift_cancelled, review), vše otestováno rollbackem. Bump `employer-main.jsx?v=25`. **Limit:** team member „acting as" owner nedostane realtime (notifikace mají `user_id=owner`, RLS `auth.uid()=user_id`) — běžný případ (owner sám) funguje. **MCP:** založen `Makej-projekt/.mcp.json` se Supabase MCP (PAT) — po restartu session bude `apply_migration`/`execute_sql` k dispozici napřímo. **Bezpečnost:** `sb_secret_…` service_role klíč i PAT byly sdíleny v chatu → **rotovat oba v dashboardu**; PAT je uložen plaintextem v `.mcp.json` (necommitne se, kořen není git repo).
- **Onboarding profilu brigádníka + gate swipování (makej-aplikace)** (2026-07): nový soubor `www/worker-onboarding.jsx` — vícekrokový wizard, který se zobrazí každému brigádníkovi s **neúplným profilem** a **nedovolí swipovat**, dokud nevyplní povinné údaje. Kroky: intro (uvítání + přehled) → 1) datum narození (`birth_date`, min 15 let) + pohlaví (`gender` = `muz`/`zena`/`jine`) → 2) kraj (`kraj`, sdílí `KRAJE_W` z `worker-swipe.jsx`) → 3) o mně (`bio`, min 10 znaků) + zkušenosti/dovednosti (`skills[]`, návrhové chipy). Uloží přes `updateProfileW`. **Gate v `worker-main.jsx`**: po `fetchWorkerData` zavolá `wOnboardingNeeded(W_PROFILE)`; když true → `body = <WOnboarding>` a **skryje spodní nav i zvoneček** (nelze se dostat na swipe). Helper `wOnboardingNeeded` = chybí-li name/birth_date/gender/kraj/bio nebo prázdné skills. **Žádná migrace** — všechna pole už v `profiles` existují. Skript přidán do `index.html` před `worker-main.jsx`. Sjednocení hodnot `gender` na `muz`/`zena`/`jine` (analytics už tak čte) → opraven i `EWorkerProfileModal` v `employer-main.jsx` (`genderTxt` mapa, bump `?v=24`). **Znovuotevření z profilu:** řádek „Osobní údaje" v sekci Nastavení (`worker-profile.jsx`) volá `window.wOpenOnboarding()` → WorkerApp otevře onboarding v „manuálním" režimu (`obManual`), kde je navíc **křížek pro zavření** (`onClose` prop, na intru i v hlavičce) a formuláře jsou předvyplněné z `W_PROFILE`. Ověřeno v prohlížeči (agent-browser): všechny 3 kroky + validace + gating logika + předvyplnění + zavření + trigger z profilu. **Pozor:** appková registrace v `index.html` tyhle údaje nesbírá, takže onboarding uvidí ~všichni, kdo se registrovali přes appku.
- **Reorganizace složek** (2026-07): `makej-web` a `makej-aplikace` jsou teď **sourozenci** ve složce `/Users/samuelpseja/cursor/Makej-projekt/` (dřív byla appka uvnitř webu jako `makej-web/makej-aplikace/`). Mobilní `makej` zůstává samostatně v `cursor/makej`. Claude Code paměť zkopírována na novou path-encoded cestu (`-Users-samuelpseja-cursor-Makej-projekt-makej-web`). Cesty aktualizované v `CLAUDE.md` i README appky. Lokální test appky: `python3 -m http.server 4000` ve `Makej-projekt/makej-aplikace/www`.
- **Plné profily místo jen recenzí (obě strany)** (2026-07): `WEmployerModal` (worker appka → profil firmy, `makej-aplikace/www/worker-main.jsx`) teď ukazuje **celý profil firmy** — hero s modrým gradientem + logo (`logo_url`), fotky firmy (`photos`), sekce O firmě (`bio`), Informace (obor `industry`, kraj, sídlo `address`, web `website`, IČO `ic`, „Na Makej od" z `created_at`), sociální sítě (`socials`), pak recenze; fallback hláška když firma nic nevyplnila. Dashboard `EWorkerProfileModal` (`employer-main.jsx`, bump `?v=23`) už plný profil měl (staty, bio, vzdělání, dovednosti, CV, recenze) — přidán jen **věk** (z `birth_date`), pohlaví a kraj do podtitulku. **Žádná migrace** — všechna pole už v `profiles` existovala.
- **Redesign vizuálu appky dle mockupu „Makej-redesign-15" (makej-aplikace)** (2026-07): kompletní vizuální předělání 4 hlavních obrazovek (swipe/Domů, Moje brigády, Zprávy, Profil) podle staženého HTML mockupu (design_doc bundle → rozbaleno v `scratchpadu`). Změny: **nové fonty** Space Grotesk (nadpisy/čísla, 800) + Instrument Sans (text) — woff2 vytaženy z bundlu do `www/fonts/` (`SpaceGrotesk-*`, `InstrumentSans-*`), `@font-face` v `fonts.css` (weight ranges). **Nová paleta v `worker-theme.jsx`**: primary `#1a34e8`, bg `#f5f7fd`, ink `#14162b`, muted `#9096ad`, + tokeny `tint`/`green`/`greenSoft`/`navBg`/`heroGrad`/`avatarGrad`. **Navbar** (`worker-main.jsx`) = tmavě-modrý pill `#14162b`, aktivní tab modrý s popiskem (zachována flex-grow animace). **Swipe karta** (`worker-swipe.jsx`): hero = radiální modrý gradient + tečkovaná textura + monogram + skleněné logo + rating pill + „NABÍRÁ TEĎ" odznak; datum/čas jako chipy; akční tlačítka pod kartou (červené X / zlatá hvězda / velké modré srdce). **Historie**: výzva k hodnocení = modrá gradientová karta, „Hotovo" zelený badge. **Zprávy**: gradient avatary (r16) + online tečka, nepřečtené řádky jako bílé karty. **Profil**: nadpis „Profil", gradient avatar, výdělek = radiální hero karta s chipem, level = **prstenec pokroku** (SVG), stat karty barevné. Login/onboarding v `index.html` sladěno (blue `#1a34e8`, Instrument Sans). Vše ověřeno parserem + preview render swipe karty v prohlížeči (sedí s mockupem). **Pozor:** změny jen v `makej-aplikace/` — web `worker/` nezměněn.
- **Onboarding + nová registrace/přihlášení (makej-aplikace)** (2026-07): 1) uvítací splash při **prvním spuštění** — obrázek `www/onboarding.png` (z `splash-11a.png`, ořezaný) jako celoobrazovkové pozadí `#onboarding`, klik = zavře + `localStorage: makej-onboarded`. 2) auth brána přepsaná ze staré tmavé na **světlou full-screen** (dle `Registrace 12b.png`): registrace (výchozí) + přihlášení, přepínání, oko u hesla, povinný souhlas, Google/Apple přes `signInWithOAuth` (fallback hláška když provider není nastaven), napojeno na stávající `sb.auth` (role `worker`, CV odebráno z registrace). Vše v `www/index.html` (inline HTML/CSS/JS, offline). Domény: zaměstnavatel by šel na `https://makej.eu/employer/` (appka je ale jen pro brigádníky — role-select se nedělá).
- **Odpovědi na recenze — obousměrné (brigádník ↔ zaměstnavatel), realtime** (2026-07): nová tabulka `review_replies` (review_id, author_id, text, created_at) + RLS (vidí/odpovídá jen účastník recenze: reviewer_id nebo reviewed_id) + přidána do `supabase_realtime` publikace. **Migrace aplikovaná přes MCP** (není v `supabase/migrations/`). Worker (`makej-aplikace/`): karta „Recenze na mě" v profilu otevře **celou stránku** (`WReviewsPage` ve `worker-profile.jsx`) se všemi recenzemi, vlákny odpovědí a inputem; funkce `fetchReviewRepliesW`/`postReviewReplyW` ve `worker-supabase.jsx` (+ `reviewerId` do W_REVIEWS). Dashboard (`employer-main.jsx` → `EWorkerProfileModal`): u každé recenze vlákno odpovědí + input (odpovídat jde jen u recenzí, které firma napsala, `r.reviewer_id === myId`). Bump `employer-main.jsx?v=22`.
- **Redesign worker appky (Xefag/produkt styl, naše barvy)** (2026-07): swipe karta = barevný modrý panel + plovoucí bílá karta + velká typografie; akční tlačítka bílá s modrou ikonou; swipe razítka modrá/bílá; profil = header s avatarem + „Lv" odznak + gradientová karta výdělku; jednotné modré avatary bez profilovky (`W_AVATAR_BG`); zprávy jedno-panelové (seznam↔vlákno). Kompletní offline sada 33 Solar ikon (`vendor/icons-solar.js`).
- **Employer: povinná pole inzerátu** (2026-07): `ENewJobModal` validuje všechna viditelná pole podle typu úvazku (`employer-main.jsx handleSubmit`), popisky mají `*`.
- **`makej-aplikace/` — Capacitor obal rozhraní pro brigádníky** (2026-07): nová složka pro nativní iOS/Android appku. `www/` = kopie worker appky (`app.jsx` + `worker-*.jsx`), ale **100 % offline** — React/ReactDOM/Supabase/Babel/iconify + fonty (latin+latin-ext) + Solar ikony (`IconifyPreload`) staženy lokálně do `www/vendor/` a `www/fonts/`. `index.html` má nativní viewport (viewport-fit=cover, no-zoom), safe-area insety, no-bounce, odstraněn „zpět na web" odkaz + GA. `capacitor.config.json` (appId `eu.makej.brigadnik`), `package.json`, README s kroky pro App Store i Google Play. Ověřeno v prohlížeči (iPhone viewport, bez chyb). **Pozor:** kód se duplikuje s `worker/` — při změně workeru přenést i sem (`cp` + `cap sync`), viz README.
- **Mobilní responzivita webu i dashboardu** (2026-07):
  - Dashboard: výsuvný drawer sidebar + hamburger v topbaru, kompaktní topbar, Zprávy přepínají seznam↔vlákno se zpět tlačítkem, gridy se stackují, fixní panely/modaly dostaly `maxWidth`.
  - Web: většina stránek už responzivní byla; doladěn ceník (balíčky 1 sloupec <600px), ověřeny ukázka dashboardu/before-after posuvník (škálují se), srovnávací tabulka scrolluje.
- Footer: odkaz „Ceník" odstraněn ze všech stránek (stránka `cenik.html` ale pořád existuje).
- Nový ceník od Yasina (yp-* karty) na `pro-zamestnavatele.html`; e-maily sjednoceny na `admin@makej.eu` (na této stránce).

## Rozdělané / ke zvážení (nedělat bez potvrzení)

- Kompaktnější pohledy na mobilu zůstávají hutné: **týdenní kalendář směn** (grid 7 sloupců) a **grafy analytiky** — funkční, ale těsné. Případně doladit.
- Sjednotit `hello@makej.eu` → `admin@makej.eu` i na ostatních stránkách (zatím jen pro-zamestnavatele).
- Exportovat DB migrace (aplikované přes Supabase MCP) do `supabase/migrations/` — zatím nejsou v gitu.
- `pro-zamestnavatele.html` má mrtvý CSS starého ceníku (`.cn-*` blok kolem ř. 258–290) — nepoužívá se.

## Na co si dát pozor (gotchas)

- **Gradient tlačítek** v mobilní app: `from-primary to-accent` (tmavě modrý). NIKDY `to-secondary`.
- `E_JOBS`, `E_THREADS` atd. jsou `const` globály — `employer-supabase.jsx` je mutuje in-place, nereassignovat.
- `storageKey: 'makej-auth'` musí být stejný ve všech Supabase klientech (sdílená session web ↔ app).
- Supabase anon/publishable key je veřejný — bezpečný ve frontend kódu.
- Commit message končí: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
